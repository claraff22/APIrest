export class Types {

    constructor(i: number, t: string, p: number){
        this.id = i;
        this.type = t;
        this.price = p;
    }

    id: number;
    type: string;
    price: number;
}